// Needs functionality to check user token, check if user is logged in, check if token is expired, getToken from local storage, set token in local storage, and
// Remove token from local storage, reference 24-Stu_Decode-JWT